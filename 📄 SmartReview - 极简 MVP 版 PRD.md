# 📄 **SmartReview - 极简 MVP 版 PRD（仅 1 个功能）**

## 一、产品名称  
**SmartReview**（简洁易记，与 AI + 评论强关联）

---

## 二、核心功能（唯一功能）  
✅ **拍照 → 填写用途 → 生成一条可直接复制的评论**

> 用户只需三步，完成从“看到物品”到“写出好评论”的全过程。

---

## 三、用户流程（极简设计）

1. **打开 App** → 主界面仅有一个按钮：「拍一张图」
2. **拍照或从相册选图** → 上传一张物品照片（如：新买的耳机、餐厅菜品、收到的礼物）
3. **填写用途** → 弹出输入框，仅一个字段：
   - “你买这个是为什么？（比如：送朋友、自用、拍照用）”
   - 提示：“简单说一句就好，比如‘生日礼物’‘日常用’”
4. **AI 生成评论** → 1 秒内生成一条自然、有细节的评论，显示在屏幕上：
   - ✅ 示例：  
     > “收到时包装很精致，朋友说这礼物有品位，用了一周，音质比预期好，推荐！”
5. **一键复制** → 屏幕下方按钮：“复制评论” → 点击后自动复制到剪贴板
6. **完成** → 可返回或分享到社交平台（可选，非必须）

---

## 四、关键设计原则

| 原则           | 实现方式                                             |
| -------------- | ---------------------------------------------------- |
| **极简**       | 仅 1 个主功能，无菜单、无设置、无历史记录            |
| **无脑操作**   | 用户只做三件事：拍、填、复制                         |
| **真实感**     | 评论必须像“人写”的，避免 AI 味（如“本产品性能卓越”） |
| **快速出结果** | 生成时间 < 1.5 秒（必须）                            |

---

## 五、AI 生成逻辑（提示词模板，供开发使用）

```text
你是一名真实用户，刚刚收到一个物品。请根据以下信息，写一条自然、口语化、带细节的评论（15–30 字）：

- 图片内容：[用户上传的物品，如：蓝色运动鞋、牛肉面、手机壳]
- 使用目的：[用户填写，如：自用、送女友、拍照用]

要求：
- 用第一人称“我”
- 加入 1 个具体细节（颜色、手感、味道、包装等）
- 避免“非常满意”“强烈推荐”等套话
- 语气像朋友聊天

示例输出：
“这双鞋上脚很轻，走路不累，朋友问是不是新买的，我笑了。”
```

---

## 六、技术实现建议（MVP 级别）

- **前端**：Flutter / React Native（跨平台，开发快）
- **AI 后端**：使用轻量级模型（如：通义千问 Qwen-Tiny、或 OpenAI GPT-3.5）
- **部署**：用 Serverless（如 AWS Lambda + API Gateway）降低运维成本
- **无需功能**：
  - 用户账户
  - 评论历史
  - 多语言
  - 评论发布按钮

---

## 七、成功指标（MVP 测试）

| 指标              | 目标                                 |
| ----------------- | ------------------------------------ |
| 用户完成率        | ≥ 85% 用户能完成“拍 → 填 → 复制”流程 |
| 评论生成时间      | ≤ 1.5 秒                             |
| 用户满意度（NPS） | ≥ 60（测试 10 个用户）               |
| 复制率            | ≥ 90% 用户点击“复制”按钮             |

---

## ✅ 总结：这个 MVP 只做一件事  
> **“拍一张图，说清楚用途，AI 生成一条像人写的评论，一键复制”**

它极简、快速、真实，适合快速上架测试市场反应。
